name: Build avatar assets

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Avatar assets are managed directly; automatic regeneration is disabled."
