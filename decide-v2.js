(() => {
  const API='https://uehmbzwnbariqebbxcst.supabase.co/functions/v1/planning-v2';
  let rendering=false,lastSig='';
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const fmtDate=d=>new Date(d+'T12:00:00').toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'short'}).toUpperCase();
  const creds=()=>({groupId:session?.groupId,token:session?.memberToken});
  async function call(action,method='GET',extra={}){const c=creds();let url=`${API}?action=${action}`;const opt={method,headers:{}};if(method==='GET'){url+=`&groupId=${encodeURIComponent(c.groupId||'')}&token=${encodeURIComponent(c.token||'')}`}else{opt.headers['content-type']='application/json';opt.body=JSON.stringify({...c,...extra})}const r=await fetch(url,opt);const j=await r.json();if(!r.ok)throw new Error(j.error||'Planning update failed');return j}
  function isDecide(){const root=document.querySelector('#planningV2');return !!root&&[...root.querySelectorAll('.steps span')].some(x=>x.classList.contains('on')&&x.textContent.includes('DECIDE'))}
  function eventMap(){const m=new Map();for(const e of (events||[]))m.set(String(e.id),e);return m}
  function myAvailable(ps,e){const mine=(ps.availability||[]).filter(a=>a.member_id===ps.me.id);if(mine.some(a=>a.dont_mind))return true;return mine.some(a=>String(a.date).slice(0,10)===e.date)}
  function voteNames(ps,eventId){const ids=(ps.votes||[]).filter(v=>v.event_id===eventId).map(v=>v.member_id);return (ps.members||[]).filter(m=>ids.includes(m.id)).map(m=>m.name)}
  function styles(){if(document.querySelector('#decideV2Style'))return;const s=document.createElement('style');s.id='decideV2Style';s.textContent=`#planningV2 .dv-summary{background:#122019;border:1px solid #355744;border-radius:16px;padding:14px 15px;margin:14px 0}#planningV2 .dv-summary strong{display:block;font-size:16px;color:#8af0b2}#planningV2 .dv-summary small{display:block;color:#aeb9b1;margin-top:5px;line-height:1.4}#planningV2 .dv-votes{font-size:12px;font-weight:900;color:#72df9e;margin-top:8px}#planningV2 .dv-names{font-size:13px;color:#dce8e0;margin-top:5px;line-height:1.45}#planningV2 .dv-card{position:relative}#planningV2 .dv-card.favourite{border-color:#72df9e}#planningV2 .dv-fav{font-size:10px;letter-spacing:.08em;font-weight:900;color:#72df9e;margin-bottom:8px}#planningV2 .dv-card h3{margin:0 0 4px;font-size:18px}#planningV2 .dv-meta{font-size:11px;color:#8f9aa1;line-height:1.5}#planningV2 .dv-btn{width:100%;margin-top:12px}#planningV2 .dv-btn.on{background:#72df9e;color:#07130c;border-color:#72df9e}#planningV2 .dv-btn:disabled{opacity:.5}#planningV2 .dv-none{color:#758089;font-size:12px;margin-top:8px}#planningV2 .dv-warning{color:#ffd166;font-size:11px;font-weight:800;margin-top:8px}#planningV2 .dv-zero{margin-top:24px;padding-top:8px;border-top:1px solid #273037}#planningV2 .dv-zero-label{font-size:11px;letter-spacing:.08em;font-weight:900;color:#758089;margin:8px 0 2px}`;document.head.appendChild(s)}
  async function render(){if(rendering||!isDecide()||!creds().groupId)return;rendering=true;try{const ps=await call('state'),map=eventMap(),ids=[...new Set((ps.interest||[]).map(i=>String(i.event_id)))],mine=(ps.votes||[]).filter(v=>v.member_id===ps.me.id),voters=new Set((ps.votes||[]).map(v=>v.member_id)),waiting=(ps.members||[]).filter(m=>!voters.has(m.id)).map(m=>m.name);const rows=ids.map(id=>({id,e:map.get(id),names:voteNames(ps,id),mine:mine.some(v=>v.event_id===id)})).filter(x=>x.e).sort((a,b)=>b.names.length-a.names.length||a.e.date.localeCompare(b.e.date));const sig=JSON.stringify([rows.map(x=>[x.id,x.names,x.mine]),waiting,mine.length]);if(sig===lastSig&&document.querySelector('#decideV2Body'))return;lastSig=sig;styles();const root=document.querySelector('#planningV2 .pv-wrap');if(!root)return;let body=document.querySelector('#decideV2Body');if(!body){body=document.createElement('div');body.id='decideV2Body';const steps=root.querySelector('.steps');if(steps){while(steps.nextSibling)steps.nextSibling.remove();steps.after(body)}else root.appendChild(body)}const max=Math.max(0,...rows.map(x=>x.names.length));const voted=rows.filter(x=>x.names.length),zero=rows.filter(x=>!x.names.length);const card=(x,i)=>{const e=x.e,unavailable=!myAvailable(ps,e),fav=x.names.length>0&&x.names.length===max;return `<div class="pv-card dv-card ${fav?'favourite':''}">${fav&&i===0?'<div class="dv-fav">🏁 CREW FAVOURITE</div>':''}<h3>${esc(e.track)} · ${esc(fmtDate(e.date))}</h3><div class="dv-meta">${esc(e.provider||'Track day')}${e.price!=null?` · £${esc(e.price)}`:''}</div>${x.names.length?`<div class="dv-votes">${x.names.length} VOTE${x.names.length===1?'':'S'}</div><div class="dv-names">${x.names.map(esc).join(' · ')}</div>`:'<div class="dv-none">No votes yet</div>'}${unavailable?'<div class="dv-warning">⚠️ You didn’t mark this date as available</div>':''}<button class="dv-btn ${x.mine?'on':''}" data-dv-vote="${esc(x.id)}" data-on="${x.mine?'1':'0'}" ${!x.mine&&mine.length>=2?'disabled':''}>${x.mine?'VOTED ✓':mine.length>=2?'2 OF 2 VOTES USED':'VOTE FOR THIS'}</button></div>`};body.innerHTML=`<div class="dv-summary"><strong>Your votes: ${mine.length} of 2</strong><small>${voters.size} of ${(ps.members||[]).length} Heros have voted${waiting.length?` · Waiting for ${waiting.map(esc).join(', ')}`:''}</small></div>${rows.length?(voted.map(card).join('')+(zero.length?`<div class="dv-zero"><div class="dv-zero-label">OTHER OPTIONS</div>${zero.map((x,i)=>card(x,voted.length+i)).join('')}</div>`:'')):'<div class="pv-card empty"><h2>No crew choices yet</h2><p>Events will appear here as Heros bring them through from Choices.</p></div>'}`;body.querySelectorAll('[data-dv-vote]').forEach(b=>b.onclick=async()=>{const id=b.dataset.dvVote,on=b.dataset.on==='1',row=rows.find(x=>x.id===id);if(!on&&row&&!myAvailable(ps,row.e)){if(!confirm('You didn’t mark this date as available. Vote for it anyway?'))return}b.disabled=true;try{await call('vote','POST',{eventId:id,voted:!on});lastSig='';await render()}catch(e){alert(e.message);b.disabled=false}})}catch(e){console.error('Decide v2',e)}finally{rendering=false}}
  const obs=new MutationObserver(()=>{if(isDecide())setTimeout(render,30)});function start(){const root=document.querySelector('#planningV2');if(!root)return setTimeout(start,300);obs.observe(root,{childList:true,subtree:true,attributes:true});if(isDecide())render()}start();window.addEventListener('focus',()=>isDecide()&&render());
})();