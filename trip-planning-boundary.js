(() => {
  // Planning is intentionally available alongside a confirmed/live Trip.
  // Global navigation decides which surface is visible; planning activity
  // must never replace or mutate the protected confirmed Trip lifecycle.
})();