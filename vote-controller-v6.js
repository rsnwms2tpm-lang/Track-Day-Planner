(function(){
  let selectedDecisionEventId='';

  const mine=id=>(state.votes||[]).find(v=>v.member_id===state.me?.id&&v.event_id===id)?.status||'';
  const memberName=id=>(state.members||[]).find(m=>m.id===id)?.name||'Unknown';
  const personalMatches=()=>rankedEvents().filter(e=>['yes','maybe'].includes(myAvailability[e.date]));
  const discussionEvents=()=>{const ids=new Set((state.votes||[]).filter(v=>['yes','maybe'].includes(v.status)).map(v=>v.event_id));return rankedEvents().filter(e=>ids.has(e.id));};

  const travelData=[
    ['castle combe','Wiltshire',150,'2h 35m'],['thruxton','Hampshire',160,'2h 45m'],['llandow','South Wales',190,'3h 20m'],['goodwood','West Sussex',200,'3h 30m'],['pembrey','Carmarthenshire',210,'3h 45m'],['silverstone','Northamptonshire',245,'4h 10m'],['mallory','Leicestershire',270,'4h 35m'],['brands hatch','Kent',270,'4h 40m'],['donington','Leicestershire',275,'4h 40m'],['bedford','Bedfordshire',290,'4h 50m'],['oulton','Cheshire',320,'5h 20m'],['blyton','Lincolnshire',330,'5h 20m'],['anglesey','Anglesey',350,'6h 10m'],['cadwell','Lincolnshire',365,'6h'],['snetterton','Norfolk',365,'6h 15m'],['croft','North Yorkshire',430,'7h'],['knockhill','Fife',520,'8h 30m']
  ];
  function travelInfo(track){const name=(track||'').toLowerCase(),m=travelData.find(([key])=>name.includes(key));if(!m)return '';const [,region,miles,time]=m;const maps=`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(track)}`;return `<div class="meta" style="margin-top:6px"><a href="${maps}" target="_blank" rel="noopener">${region}</a> · ~${miles} miles · ~${time} from Cornwall Services</div>`;}
  function availabilityBadge(e){const av=myAvailability[e.date]||'';return `<div class="my-availability-badge ${av||'unmarked'}">${av==='yes'?'✓ YOU MARKED THIS DATE AVAILABLE':av==='maybe'?'? YOU MARKED THIS DATE MAYBE':'— YOU DID NOT MARK THIS DATE AVAILABLE'}</div>`;}

  async function refreshGroup(){state=await api('group','GET',{groupId:session.groupId,token:session.memberToken});myAvailability={};(state.availability||[]).filter(a=>a.member_id===state.me.id).forEach(a=>myAvailability[String(a.date).slice(0,10)]=a.status);}
  function voteButtons(e){const selected=mine(e.id);return `<div class="vote-row vote-three"><button type="button" data-vote-choice="yes" data-event-id="${e.id}" class="${selected==='yes'?'selected':''}">Yes</button><button type="button" data-vote-choice="maybe" data-event-id="${e.id}" class="${selected==='maybe'?'selected':''}">Maybe</button><button type="button" data-vote-choice="no" data-event-id="${e.id}" class="${selected==='no'?'selected':''}">Can’t do</button></div>`;}
  function bindVotes(root){root.querySelectorAll('[data-vote-choice]').forEach(btn=>btn.onclick=async()=>{const row=btn.closest('.vote-row'),card=btn.closest('.event');if(row?.dataset.saving==='1')return;const eventId=btn.dataset.eventId,choice=btn.dataset.voteChoice,clear=mine(eventId)===choice;row.dataset.saving='1';row.querySelectorAll('[data-vote-choice]').forEach(b=>b.classList.remove('selected'));if(!clear)btn.classList.add('selected');const score=card?.querySelector('.score strong');if(score)score.textContent=clear?'—':choice==='yes'?'Yes':choice==='maybe'?'Maybe':'Can’t do';try{await api('vote','POST',{groupId:session.groupId,token:session.memberToken,eventId,...(clear?{clear:true}:{status:choice})});await refreshGroup();renderVotes();renderConfirmation();progress();}catch(err){alert('Could not update choice: '+err.message);renderVotes();renderConfirmation();}});}
  function choiceCard(e){const dt=new Date(e.date+'T12:00:00'),selected=mine(e.id),price=e.price!=null?`£${e.price}`:'Price TBC';return `<article class="card event"><div class="datebox"><strong>${dt.getDate()}</strong><span>${dt.toLocaleString('en-GB',{month:'short'}).toUpperCase()} ${dt.getFullYear()}</span></div><div><h3>${e.track}</h3><div class="meta">${e.provider} · ${e.format} · ${price}</div>${travelInfo(e.track)}${availabilityBadge(e)}${voteButtons(e)}</div><div class="score"><strong>${selected==='yes'?'Yes':selected==='maybe'?'Maybe':selected==='no'?"Can’t do":'—'}</strong><span class="meta">your choice</span></div></article>`;}
  renderVotes=function(){const el=document.querySelector('#voteList'),next=document.querySelector('#confirmWinnerBtn'),status=document.querySelector('#voteStatus');if(!el||!next||!state.me)return;const list=personalMatches();if(!events.length){el.innerHTML='<div class="card"><p class="muted">Live events are still loading.</p></div>';next.disabled=true;return;}if(!list.length){el.innerHTML='<div class="card"><h3>No track days match your dates yet.</h3><p class="muted">Go back to Availability and add some dates to build your personal shortlist.</p></div>';next.disabled=true;if(status)status.textContent='NO CHOICES YET';return;}el.innerHTML=`<div class="choice-section"><span class="eyebrow">YOUR MATCHES</span><p class="muted">These track days match dates you marked Available or Maybe.</p></div>${list.map(choiceCard).join('')}`;bindVotes(el);const answered=list.filter(e=>mine(e.id)).length;if(status)status.textContent=`${answered}/${list.length} ANSWERED`;next.disabled=false;next.onclick=()=>stage('confirm');};
  function decisionInfo(){const members=state.members||[];return discussionEvents().map(e=>{const votes=(state.votes||[]).filter(v=>v.event_id===e.id),names=status=>votes.filter(v=>v.status===status).map(v=>memberName(v.member_id)),yes=names('yes'),maybe=names('maybe'),no=names('no'),answered=new Set(votes.map(v=>v.member_id)),unanswered=members.filter(m=>!answered.has(m.id)).map(m=>m.name);return {e,yes,maybe,no,unanswered,favourite:members.length>0&&!unanswered.length&&!no.length};});}
  function shareSummary(x){const e=x.e,date=new Date(e.date+'T12:00:00').toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'short',year:'numeric'}),price=e.price!=null?`£${e.price}`:'Price TBC';return `🏁 Track Day Heros — ${e.track}\n${date} · ${price}\n\n👍 Yes (${x.yes.length}): ${x.yes.join(', ')||'Nobody'}\n🤔 Maybe (${x.maybe.length}): ${x.maybe.join(', ')||'Nobody'}\n❌ Can’t do (${x.no.length}): ${x.no.join(', ')||'Nobody'}${x.unanswered.length?`\n⏳ No response (${x.unanswered.length}): ${x.unanswered.join(', ')}`:''}\n\nWhat do we reckon?`;}
  async function shareDecision(x,button){const text=shareSummary(x);try{if(navigator.share){await navigator.share({title:`${x.e.track} — Track Day Heros`,text});return;}await navigator.clipboard.writeText(text);const old=button.textContent;button.textContent='Copied — paste into group chat';setTimeout(()=>button.textContent=old,1800);}catch(err){if(err?.name!=='AbortError')alert('Could not share this choice: '+err.message);}}
  async function confirmSelected(){const x=decisionInfo().find(x=>x.e.id===selectedDecisionEventId);if(!x)return;let warning=`Confirm ${x.e.track} as the group’s track day?`;if(x.maybe.length)warning+=`\n\nMaybe: ${x.maybe.join(', ')}`;if(x.no.length)warning+=`\nCan’t do: ${x.no.join(', ')}`;warning+='\n\nThis should be used after you’ve agreed it in the group chat.';if(!window.confirm(warning))return;const btn=document.querySelector('#finalConfirmBtn');if(btn)btn.disabled=true;try{await api('confirm','POST',{groupId:session.groupId,token:session.memberToken,eventId:x.e.id});await refreshGroup();selectedDecisionEventId='';renderTrip();progress();stage('trip');}catch(err){alert('Could not confirm track day: '+err.message);renderConfirmation();}}
  function decisionCard(x,best){const e=x.e,dt=new Date(e.date+'T12:00:00'),price=e.price!=null?`£${e.price}`:'Price TBC',selected=e.id===selectedDecisionEventId,current=mine(e.id);return `<article class="card event confirmation-choice ${selected?'selected':''}"><div class="datebox"><strong>${dt.getDate()}</strong><span>${dt.toLocaleString('en-GB',{month:'short'}).toUpperCase()} ${dt.getFullYear()}</span></div><div>${x.favourite?`<div class="pill success">⭐ GROUP FAVOURITE${best===e.id?' · 🏆 BEST MATCH':''}</div>`:''}<h3>${e.track}</h3><div class="meta">${e.provider} · ${e.format} · ${price}</div>${travelInfo(e.track)}<div class="confirmation-breakdown"><div><strong>Yes (${x.yes.length}/${(state.members||[]).length})</strong><span> · ${x.yes.join(', ')||'Nobody'}</span></div><div><strong>Maybe (${x.maybe.length})</strong><span> · ${x.maybe.join(', ')||'Nobody'}</span></div><div><strong>Can’t do (${x.no.length})</strong><span> · ${x.no.join(', ')||'Nobody'}</span></div>${x.unanswered.length?`<div><strong>Needs a response (${x.unanswered.length})</strong><span> · ${x.unanswered.join(', ')}</span></div>`:'<div><strong>✓ Everyone responded</strong></div>'}</div>${availabilityBadge(e)}<div class="meta" style="margin:6px 0 4px">${current?'Your current choice: '+(current==='yes'?'Yes':current==='maybe'?'Maybe':'Can’t do'):'You haven’t chosen yet'}</div>${voteButtons(e)}<div class="vote-row"><button type="button" data-share="${e.id}">Share to group chat</button><button type="button" data-select="${e.id}" class="${selected?'selected':''}">${selected?'Deselect track day':'Confirm this track day'}</button></div></div><div class="score"><strong>${x.yes.length}</strong><span class="meta">yes · ${x.maybe.length} maybe</span></div></article>`;}
  function renderConfirmation(){const el=document.querySelector('#confirmationList'),confirmBtn=document.querySelector('#finalConfirmBtn'),status=document.querySelector('#confirmationStatus');if(!el||!confirmBtn||!state.me)return;const info=decisionInfo();if(!info.length){el.innerHTML='<div class="card"><h3>No group choices yet.</h3><p class="muted">Once somebody chooses Yes or Maybe in Choices, it’ll appear here for everyone to respond to.</p></div>';confirmBtn.disabled=true;return;}const favourites=info.filter(x=>x.favourite).sort((a,b)=>b.yes.length-a.yes.length||a.maybe.length-b.maybe.length),best=favourites[0]&&favourites.filter(x=>x.yes.length===favourites[0].yes.length&&x.maybe.length===favourites[0].maybe.length).length===1?favourites[0].e.id:'',live=info.filter(x=>!x.favourite&&!x.no.length),ruled=info.filter(x=>x.no.length);if(selectedDecisionEventId&&!info.some(x=>x.e.id===selectedDecisionEventId))selectedDecisionEventId='';const section=(title,items)=>items.length?`<div class="choice-section"><span class="eyebrow">${title}</span></div>${items.map(x=>decisionCard(x,best)).join('')}`:'';el.innerHTML=`<div class="card"><span class="eyebrow">GROUP STATUS</span><h3>${(state.members||[]).length} in group</h3><p class="muted">See everyone’s picks here, check whether you marked the date available, then change or add your own response whenever you need to.</p></div>${section('GROUP FAVOURITES',favourites)}${section('STILL IN PLAY',live)}${section('RULED OUT',ruled)}`;bindVotes(el);const byId=new Map(info.map(x=>[x.e.id,x]));el.querySelectorAll('[data-share]').forEach(btn=>btn.onclick=()=>shareDecision(byId.get(btn.dataset.share),btn));el.querySelectorAll('[data-select]').forEach(btn=>btn.onclick=()=>{selectedDecisionEventId=selectedDecisionEventId===btn.dataset.select?'':btn.dataset.select;renderConfirmation();});const selected=byId.get(selectedDecisionEventId);confirmBtn.disabled=!selected;confirmBtn.textContent=selected?`Confirm ${selected.e.track} →`:'Discuss above, then confirm your choice';confirmBtn.onclick=selected?confirmSelected:null;if(status)status.textContent=selected?'TRACK DAY SELECTED':favourites.length?`${favourites.length} GROUP FAVOURITE${favourites.length===1?'':'S'}`:'READY TO DISCUSS';}

  const heroCopy={availability:['Right lads, where to next? 🏁','Get your free dates in and we’ll see what lines up.'],vote:['Decisions, decisions… 👀','Pick the track days that work for you.'],confirm:["Who’s the awkward one then? 👀","See everyone’s picks, check your availability and work out the best fit."],trip:["Let’s get it booked! 🏁",'']};
  function updateHero(id){const copy=heroCopy[id]||heroCopy.availability,title=document.querySelector('#heroTitle'),text=document.querySelector('#heroText');if(title)title.textContent=copy[0];if(text){text.textContent=copy[1];text.hidden=!copy[1];}}
  const baseStage=stage;
  stage=function(id){baseStage(id);updateHero(id);if(id==='vote')renderVotes();if(id==='confirm')renderConfirmation();const hero=document.querySelector('.hero');if(hero){const top=hero.getBoundingClientRect().top+window.scrollY-8;requestAnimationFrame(()=>window.scrollTo({top,behavior:'auto'}));}};
  const baseRender=render;render=function(){baseRender();renderConfirmation();};
  document.querySelectorAll('[data-stage]').forEach(btn=>btn.onclick=()=>stage(btn.dataset.stage));
  updateHero('availability');if(state.me){renderVotes();renderConfirmation();}
})();